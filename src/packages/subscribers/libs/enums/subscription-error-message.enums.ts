const SubscriptionErrorMessage = {
  SUBSCRIPTION_EXIST: 'Subscription with such email already exists.',
  SUBSCRIPTION_NOT_FOUND: 'Subscription not found.',
} as const;

export { SubscriptionErrorMessage };
